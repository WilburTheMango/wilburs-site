const cardsDirectory = '../images/tarot';

const urlParams = new URLSearchParams(window.location.search);
const seed = urlParams.get('seed');
const files = [];
// for (let i = 0; i < )

console.log(cards);
